export interface IWorldSetting {
   cellAvailableSteps: number,
   initializeCells: number,
   worldWidth: number,
   worldHeight: number,
   cellSize: number,
   mainDiv: string,
   mapBackgroundColor: number,
   allCellsColors: number[]
}
